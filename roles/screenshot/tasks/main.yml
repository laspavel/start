---
- name: Create local tmp folder
  file:
    src: "/dev/shm"
    dest: "{{ work_dir }}/tmp"
    state: link

- name: Deploy screenshot script ssarea.sh
  copy:
    src: "ssarea.sh"
    dest: "/usr/bin/ssarea.sh"
    owner: root
    group: root
    mode: 0755
