---
    - name: StartMyworkSystem
      hosts: localhost
      connection: local
      become: true
      remote_user: root
      gather_facts: true
      vars_files:
        - ./vars/config-srv_debian_user.yml
      roles:
        - role: base
        - role: chrony
        - role: sysctl
        - role: gen_rsa_key
        - role: docker
        - role: nginx
        - role: certbot
        - role: tmux
        - role: profile_conf
        - role: utils_and_tools
#        - role: optimize
#        - role: pib
#        - role: reboot

